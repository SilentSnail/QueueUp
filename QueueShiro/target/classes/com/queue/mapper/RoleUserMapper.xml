<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.queue.mapper.RoleUserMapper">

    <!-- 开启二级缓存 -->
    <!--<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>-->

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.queue.entity.RoleUser">
        <id column="user_code" property="userCode" />
        <result column="user_id" property="userId" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="role_id" property="roleId" />
        <result column="is_delete" property="isDelete" />
        <result column="create_time" property="createTime" />
        <result column="creator" property="creator" />
        <result column="update_time" property="updateTime" />
        <result column="reviser" property="reviser" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        user_id, user_code, username, password, phone, email, role_id, is_delete, create_time, creator, update_time, reviser
    </sql>

    <select id="getUserByEntity" parameterType="com.queue.entity.RoleUser" resultMap="BaseResultMap">
        select * from ROLE_USER WHERE 1 = 1
        <if test="userName != null">
            and USERNAME = #{userName,jdbcType=CHAR}
        </if>
        <if test="email != null">
            and email = #{email,jdbcType=VARCHAR}
        </if>
    </select>

    <update id="changePassword">
        UPDATE ROLE_USER SET PASSWORD = #{password} WHERE USER_ID = #{userId}
    </update>

</mapper>
